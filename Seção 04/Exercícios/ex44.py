step = float(input('Step height: '))
height = float(input('Height that the user wants to reach by climbing the ladder: '))
steps = int(height / step)

print(f'Steps that the user must climb to reach their goal: {steps}')
